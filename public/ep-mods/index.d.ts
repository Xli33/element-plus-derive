/**
 * 为el-table添加列宽缓存功能，由prop: cache-cols属性启用
 */
declare module 'element-plus-derive/ep-mods/table-cache-cols' {
  // 为 element-plus 导出的类型 TableProps 添加 cacheCols 属性声明，目前非必要故注释掉
  /* declare module 'element-plus' {
    export interface TableProps {
      cacheCols?: {
        keys: string
        cols: any[]
      }
    }
  } */
}
